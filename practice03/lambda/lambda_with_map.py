#1
numbers = [1, 2, 3, 4, 5]
doubled = list(map(lambda x: x * 2, numbers))
print(doubled)

#2
tripled = list(map(lambda x: x * 3, numbers))
print(tripled)

#3
quadrupled = list(map(lambda x: x * 4, numbers))
print(quadrupled)

#4
quintupled = list(map(lambda x: x * 5, numbers))
print(quintupled)

#5
sextupled = list(map(lambda x: x * 6, numbers))
print(sextupled)